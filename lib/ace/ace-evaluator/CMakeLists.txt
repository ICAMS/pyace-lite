cmake_minimum_required(VERSION 3.7)  # CMake version check
project(ace-evaluator)
set(CMAKE_CXX_STANDARD 11) # Enable c++11 standard

set(EVALUATOR_PATH ${CMAKE_CURRENT_LIST_DIR})
message("EVALUATOR_PATH=${EVALUATOR_PATH}")
set(EVALUATOR_SRC_PATH ${EVALUATOR_PATH}/src)


FILE(GLOB EVALUATOR_SOURCE_FILES ${EVALUATOR_SRC_PATH}/*.cpp)
set(EVALUATOR_INCLUDE_DIR ${EVALUATOR_SRC_PATH})

FILE(GLOB EVALUATOR_EXTRA_FILES ${EVALUATOR_PATH}/extra/*.cpp)
set(EVALUATOR_EXTRA_INCLUDE_DIR ${EVALUATOR_PATH}/extra/)
FILE(GLOB TEST_EVALUATOR ${EVALUATOR_PATH}/test/test_*.cpp)

#utils-test
set(EVALUATOR_EXTRA_UTILS_INCLUDE_DIR ${EVALUATOR_PATH}/extra/utils)
FILE(GLOB EVALUATOR_EXTRA_UTILS_FILES ${EVALUATOR_PATH}/extra/utils/*.cpp)